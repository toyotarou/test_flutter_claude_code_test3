# Flutter 競馬成績アプリ 詳細設計書

> **この設計書について**
>
> 詳細設計書とは「プログラムをどう作るか」を書いたドキュメントです。
> 基本設計書（何を作るか）の次のステップにあたり、
> 開発者がコードを書くために必要な情報をすべて記載します。
>
> 主に以下の観点で構成されます。
>
> | セクション | 書く内容 | なぜ必要か |
> |---|---|---|
> | 1. システム概要 | アプリ全体の目的と構成 | 新メンバーが全体像を把握するため |
> | 2. アーキテクチャ | 技術選定と設計方針 | 「なぜこう作ったか」を残すため |
> | 3. ディレクトリ構成 | ファイルの配置ルール | どこに何があるか迷わないため |
> | 4. データモデル | データの型と構造 | APIやDBとの整合性を保つため |
> | 5. 状態管理 | Providerの一覧と役割 | データの流れを明確にするため |
> | 6. 画面設計 | 各画面のUI構成と動作 | 実装の仕様を明確にするため |
> | 7. API連携 | エンドポイントと通信仕様 | サーバーとの契約を明文化するため |
> | 8. 画面遷移 | 画面間のナビゲーション | ユーザー体験の全体像を把握するため |

---

## 1. システム概要

> **このセクションの目的：** プロジェクトに初めて触れる人が「これは何のアプリか」を理解できるようにする。

### 1.1 アプリの目的

競馬のレース結果を閲覧・検索するためのモバイルアプリケーション。
Go言語で構築されたAPIサーバーからデータを取得し、一覧表示・検索・グラフ表示を行う。

### 1.2 主な機能

| 機能 | 概要 |
|---|---|
| レース一覧表示 | 全レースを日付順に一覧表示。年でフィルタリング可能 |
| レース結果表示 | レースをタップすると着順・馬名・騎手名・タイムを表示 |
| 馬名検索 | ひらがな入力対応のインクリメンタルサーチ |
| 馬名一覧 | 全馬名をあいうえお順にグループ化して表示 |
| 成績グラフ | 馬の着順推移を折れ線グラフで可視化 |

### 1.3 動作環境

| 項目 | 値 |
|---|---|
| フレームワーク | Flutter 3.38.9 (Dart 3.10.8) |
| 対応OS | Android / iOS |
| 画面方向 | 縦固定 (portraitUp) |
| APIサーバー | http://49.212.175.205:8080 (Go言語) |

---

## 2. アーキテクチャ（設計方針）

> **このセクションの目的：** 「なぜこのライブラリを使うのか」「どういうルールでコードを書くのか」を明文化する。後から参加したメンバーが勝手に違う方針で書くのを防ぐ。

### 2.1 技術スタック

```
┌─────────────────────────────────────────────┐
│                   UI層                       │
│  Widget (StatelessWidget / StatefulWidget)   │
│  flutter_hooks (useXxx でライフサイクル管理)    │
├─────────────────────────────────────────────┤
│                 状態管理層                     │
│  Riverpod (@riverpod アノテーション方式)        │
│  コード生成: riverpod_generator + build_runner │
├─────────────────────────────────────────────┤
│                データモデル層                   │
│  freezed (イミュータブルモデル)                  │
│  json_serializable (JSON変換)                │
├─────────────────────────────────────────────┤
│                 通信層                        │
│  http パッケージ (REST API通信)               │
└─────────────────────────────────────────────┘
         ↕  HTTP (JSON)
┌─────────────────────────────────────────────┐
│          Go APIサーバー (外部)                 │
│          MySQL データベース                    │
└─────────────────────────────────────────────┘
```

### 2.2 設計ルール

| ルール | 説明 |
|---|---|
| Widget の基底クラス | Providerを使う画面は `HookConsumerWidget` または `ConsumerWidget` を使う |
| 状態管理 | `@riverpod` アノテーション + コード生成。手書きProviderは使わない |
| モデル | `@freezed` で定義。イミュータブル（不変）を保証する |
| API通信 | Provider内で `http.get` を呼び出す。Widgetから直接APIを叩かない |
| テーマ | ダークテーマ統一。カラーコードは後述のカラーパレットに従う |

### 2.3 カラーパレット

> **補足：** 設計書にカラーコードを載せておくと、新しい画面を追加するときに統一感を保てる。

| 用途 | カラーコード | 色名 |
|---|---|---|
| 背景（最も暗い） | `#0F0F1A` | ほぼ黒 |
| 背景（カード・入力欄） | `#1A1A2E` | ダークネイビー |
| 背景（ヘッダー・ボタン） | `#0F3460` | ディープブルー |
| 背景（サジェスト・グループ） | `#16213E` | ダークブルー |
| アクセント（日付・リンク） | `#53C0F0` | シアン |
| エラー | `#E94560` | レッド |
| G1バッジ | `#FFD700` → `#FFA000` | ゴールド(グラデーション) |
| G2バッジ | `#C0C0C0` → `#9E9E9E` | シルバー(グラデーション) |
| G3バッジ | `#CD7F32` → `#8D6E63` | ブロンズ(グラデーション) |
| コース種別（芝） | `#2E7D32` | グリーン |
| コース種別（ダート） | `#8D6E63` | ブラウン |

---

## 3. ディレクトリ構成

> **このセクションの目的：** 「新しいファイルをどこに置けばいいか」を明確にする。

```
lib/
├── main.dart                  … アプリのエントリーポイント
├── config/
│   └── api_config.dart        … API接続設定（ベースURL等）
├── models/                    … データモデル（freezed）
│   ├── hourse_race_list.dart      … レース一覧モデル
│   ├── hourse_race_result.dart    … レース結果モデル
│   └── hourse_name_stats.dart     … 馬名統計モデル
├── providers/                 … 状態管理（Riverpod Provider）
│   └── race_provider.dart         … 全Provider + ユーティリティ関数
├── screens/                   … 画面（ページ単位のWidget）
│   └── home_screen.dart           … ホーム画面
└── widgets/                   … 再利用可能なUIパーツ
    ├── search_bar_widget.dart         … 検索バー + サジェスト
    ├── race_result_dialog.dart        … レース結果ダイアログ
    ├── horse_search_result_dialog.dart … 馬名検索結果ダイアログ
    ├── horse_name_list_dialog.dart     … 馬名一覧ダイアログ
    └── result_chart_painter.dart      … 着順推移グラフ描画
```

### 配置ルール

| ディレクトリ | 置くもの | 置かないもの |
|---|---|---|
| `config/` | 環境設定、定数定義 | ビジネスロジック |
| `models/` | freezedモデルのみ | UI関連コード |
| `providers/` | @riverpod Provider、API通信 | Widget |
| `screens/` | ページ全体を構成するWidget | 他の画面から再利用するパーツ |
| `widgets/` | 再利用可能なUIパーツ | ページ全体の構成 |

---

## 4. データモデル定義

> **このセクションの目的：** APIから受け取るデータの「型」を明確にする。サーバー側とクライアント側で「このフィールドは何型か」を合意するための仕様書になる。

### 4.1 HourseRaceList（レース一覧）

用途：ホーム画面のレース一覧に使用

| フィールド名 | JSON キー | Dart型 | 必須 | 説明 |
|---|---|---|---|---|
| id | id | `int` | Yes | レースID |
| year | year | `String?` | No | 開催年 (例: "2026") |
| month | month | `String?` | No | 開催月 (例: "01") |
| day | day | `String?` | No | 開催日 (例: "05") |
| grade | grade | `String?` | No | グレード (例: "g1", "g2", "g3") |
| raceName | race_name | `String?` | No | レース名 (例: "有馬記念") |
| place | place | `String?` | No | 開催場所 (例: "中山") |
| ageRate | age_rate | `String?` | No | 年齢条件 (例: "3歳以上") |
| courseKind | course_kind | `String?` | No | コース種別 ("芝" or "ダート") |
| courseLength | course_length | `String?` | No | 距離 (例: "2500") |
| resultUrl | result_url | `String?` | No | 結果ページURL |
| finish | finish | `int?` | No | 完了フラグ |

### 4.2 HourseRaceResult（レース結果）

用途：レース結果ダイアログ、馬名検索結果に使用

| フィールド名 | JSON キー | Dart型 | 必須 | 説明 |
|---|---|---|---|---|
| id | id | `int` | Yes | レコードID |
| year | year | `String?` | No | 開催年 |
| month | month | `String?` | No | 開催月 |
| day | day | `String?` | No | 開催日 |
| grade | grade | `String?` | No | グレード |
| raceName | race_name | `String?` | No | レース名 |
| result | result | `int?` | No | 着順 (1〜) |
| hourseName | hourse_name | `String?` | No | 馬名 |
| age | age | `String?` | No | 馬齢 |
| jockeyName | jockey_name | `String?` | No | 騎手名 |
| raceTime | race_time | `String?` | No | タイム |

### 4.3 HourseNameStats（馬名統計）

用途：馬名一覧ダイアログ、検索サジェストに使用

| フィールド名 | JSON キー | Dart型 | 必須 | 説明 |
|---|---|---|---|---|
| hourseName | hourse_name | `String` | Yes | 馬名 |
| raceCount | race_count | `int` | Yes | 出走回数 |
| lastYear | last_year | `String` | Yes | 最終出走年 (例: "2026") |

---

## 5. 状態管理設計（Provider一覧）

> **このセクションの目的：** アプリ内でデータがどこで取得され、どこで使われるかを明確にする。「このデータはどのProviderから取ればいいの？」という疑問に答える。

### 5.1 Provider一覧

```
┌──────────────────────────────────────────────────────────────┐
│                    Provider一覧                               │
├──────────────────────┬───────────────┬───────────────────────┤
│ Provider名           │ 種別           │ 返却型                │
├──────────────────────┼───────────────┼───────────────────────┤
│ allRaceProvider      │ FutureProvider │ List<HourseRaceList>  │
│ allHourseNamesProvider│ FutureProvider│ List<String>          │
│ allHourseNamesWithStatsProvider │ FutureProvider│ List<HourseNameStats>│
│ selectedYearProvider │ NotifierProvider│ String?              │
│ selectedResultProvider│ FamilyProvider│ List<HourseRaceResult>│
│ resultByHourseNameProvider│ FamilyProvider│ List<HourseRaceResult>│
└──────────────────────┴───────────────┴───────────────────────┘
```

### 5.2 各Providerの詳細

#### allRaceProvider

```
種別:        FutureProvider（非同期で1回取得）
APIエンドポイント: GET /getAllRace
返却型:      Future<List<HourseRaceList>>
使用箇所:    HomeScreen（レース一覧表示）
再取得:      エラー時に ref.invalidate(allRaceProvider) で再取得
```

#### selectedYearProvider

```
種別:        NotifierProvider（ユーザー操作で値が変わる）
返却型:      String?（null = 全年表示）
初期値:      null
操作メソッド: select(String? year) → 年フィルターを設定/解除
使用箇所:    HomeScreen（年フィルターの状態管理）
```

#### allHourseNamesProvider

```
種別:        FutureProvider
APIエンドポイント: GET /getAllHourseNames
返却型:      Future<List<String>>
使用箇所:    現在は未使用（allHourseNamesWithStatsProviderに置き換え済み）
```

#### allHourseNamesWithStatsProvider

```
種別:        FutureProvider
APIエンドポイント: GET /getAllHourseNamesWithStats
返却型:      Future<List<HourseNameStats>>
使用箇所:    HomeScreen → SearchBarWidget（検索サジェスト）
            HorseNameListDialog（馬名一覧）
```

#### selectedResultProvider（Family）

```
種別:        FamilyProvider（パラメータ付きProvider）
パラメータ:   year, month, day, raceName（すべてString、named parameter）
APIエンドポイント: GET /getSelectedResult?year=...&month=...&day=...&race_name=...
返却型:      Future<List<HourseRaceResult>>
使用箇所:    RaceResultDialog（レース結果ダイアログ）
```

#### resultByHourseNameProvider（Family）

```
種別:        FamilyProvider
パラメータ:   hourseName（String、named parameter）
APIエンドポイント: GET /getResultByHourseName?hourse_name=...
返却型:      Future<List<HourseRaceResult>>
使用箇所:    HorseSearchResultDialog（馬名検索結果ダイアログ）
```

### 5.3 ユーティリティ関数

#### toKatakana(String input)

```
目的:   ひらがなをカタカナに変換する
仕様:   Unicode のひらがな範囲 (U+3041〜U+3096) に +0x60 して
        カタカナ範囲 (U+30A1〜U+30F6) に変換
入力例: "あいうえお" → "アイウエオ"
使用箇所: SearchBarWidget（サジェスト検索時）
         HomeScreen._onSearch（検索実行時）
```

---

## 6. 画面設計

> **このセクションの目的：** 各画面の構成要素、表示ルール、操作時の振る舞いを定義する。開発者はこのセクションを見ればUIを実装できる。

### 6.1 エントリーポイント (main.dart)

```
初期化処理:
  1. WidgetsFlutterBinding.ensureInitialized()
  2. 画面方向を縦固定に設定
  3. ProviderScope でアプリ全体をラップ
  4. MyApp を起動

MyApp:
  - テーマ: ダーク
  - ホーム画面: HomeScreen
  - デバッグバナー: 非表示
```

### 6.2 ホーム画面 (HomeScreen)

**Widgetの種別:** HookConsumerWidget

**使用するHooks:**
- `useTextEditingController()` — 検索テキストフィールドの状態管理
- `useScrollController()` — レースリストのスクロール位置管理

**画面レイアウト:**

```
┌─────────────────────────────┐
│         AppBar              │
│      "Horse Race"           │
│   (グラデーション背景)        │
├─────────────────────────────┤
│ [テキスト入力   ][🔍][📋]   │  ← SearchBarWidget
│ [サジェスト候補リスト     ]   │     (入力中のみ表示)
├─────────────────────────────┤
│ (2024)(2025)(2026)          │  ← 年セレクター (横スクロール)
├─────────────────────────────┤
│ 2026/01/05  有馬記念    芝   │  ← レース一覧
│             G1        2500m │     (縦スクロール)
│─────────────────────────────│
│ 2026/01/12  京成杯     芝   │
│                       2000m │
│─────────────────────────────│
│ ...                         │
└─────────────────────────────┘
```

**状態別の表示:**

| 状態 | 表示内容 |
|---|---|
| 読み込み中 | スピナー + "Loading..." テキスト |
| エラー | エラーアイコン + エラーメッセージ + リトライボタン |
| データあり | 検索バー + 年セレクター + レースリスト |

**年セレクターの仕様:**

| 操作 | 動作 |
|---|---|
| 未選択の年をタップ | その年でフィルター。リストを先頭にスクロール |
| 選択中の年をタップ | フィルター解除（全年表示）。リストを先頭にスクロール |

**レースリストアイテムの構成:**

```
┌─────────────────────────────────────────┐
│ 2026/01/05  [G1] 有馬記念         [芝]  │
│             中山  3歳以上        2500 m  │
└─────────────────────────────────────────┘
```

| 要素 | 表示ルール |
|---|---|
| 日付 | `YYYY/MM/DD` 形式。シアン色。等幅数字 |
| gradeバッジ | G1=金グラデーション+発光, G2=銀, G3=銅, その他=グレー。大文字 |
| レース名 | 白色、太字。省略時は `...` |
| 開催場所 | 白60%透過 |
| コース種別 | 芝=緑バッジ、ダート=茶バッジ |
| 距離 | 白70%透過。末尾に "m" |

**ソート順:** 日付昇順 → 同日はレース名昇順

### 6.3 検索バー (SearchBarWidget)

**Widgetの種別:** StatefulWidget

**入力仕様:**

| 仕様 | 内容 |
|---|---|
| プレースホルダー | "馬名を入力（ひらがなOK）" |
| 入力変換 | ひらがな → カタカナに自動変換してサジェスト |
| クリアボタン | テキストが空でないときのみ表示 |

**サジェストリストの仕様:**

```
┌─────────────────────────────┐
│ アーモンドアイ        15走   │  ← アクティブ (白)
│ アカイイト             8走   │  ← アクティブ (白)
│ アグネスタキオン       5走   │  ← 非アクティブ (グレー)
└─────────────────────────────┘
```

| 仕様 | 内容 |
|---|---|
| 最大高さ | 200px（超えるとスクロール） |
| フィルタリング | 前方一致（入力文字で始まる馬名のみ） |
| 表示件数 | 制限なし（該当する全件表示） |
| 各項目の構成 | 左: 馬名、右: 出走数 (「N走」) |
| アクティブ判定 | 最終出走年 >= 今年 - 1 なら白、それ以外はグレー |
| タップ時 | テキスト欄に馬名をセット → 自動で検索実行 |

**ボタン:**

| ボタン | アイコン | 動作 |
|---|---|---|
| 検索 | 🔍 (Icons.search) | キーボードを閉じ、検索結果ダイアログを表示 |
| 一覧 | 📋 (Icons.list) | 馬名一覧ダイアログを表示 |

### 6.4 レース結果ダイアログ (RaceResultDialog)

**Widgetの種別:** ConsumerWidget

**起動条件:** ホーム画面のレースリストアイテムをタップ

**ダイアログ構成:**

```
┌─────────────────────────────┐
│ ┌─────────────────────────┐ │
│ │      有馬記念            │ │  ← ヘッダー（グラデーション背景）
│ │ 2026/01/05  中山        │ │
│ │ 芝 2500m  3歳以上  G1   │ │
│ └─────────────────────────┘ │
│                             │
│  1  ドウデュース   3:23.4   │  ← 結果リスト
│     4歳  武豊              │
│  2  スターズオンアース 3:23.6│
│     5歳  ルメール          │
│  ...                       │
└─────────────────────────────┘
```

**ヘッダー情報:**

| 要素 | 内容 |
|---|---|
| 1行目 | レース名（大きく太字） |
| 2行目 | 日付、開催場所 |
| 3行目 | コース種別、距離、年齢条件、グレード |

**結果リストアイテム:**

| 要素 | 表示ルール |
|---|---|
| 着順 | 1着=金, 2着=銀, 3着=銅, 4着以下=グレー |
| 馬名 | 白色、太字 |
| 馬齢・騎手名 | 白60%透過、小さめ |
| タイム | 白70%透過、等幅数字 |

**最大高さ:** 画面高さの75%

### 6.5 馬名検索結果ダイアログ (HorseSearchResultDialog)

**Widgetの種別:** ConsumerWidget

**起動条件:** 検索バーで検索実行時、またはサジェスト/馬名一覧からタップ時

**ダイアログ構成:**

```
┌───────────────────────────────────┐
│ ┌───────────────────────────────┐ │
│ │         ドウデュース           │ │  ← ヘッダー
│ └───────────────────────────────┘ │
│                                   │
│  ┌───────────────────────────┐   │
│  │  着順推移グラフ             │   │  ← 2件以上のとき表示
│  │  (ResultChartPainter)      │   │
│  └───────────────────────────┘   │
│                                   │
│  1  [G1] 2026/01/05      3:23.4  │  ← 結果リスト
│          有馬記念                  │
│  3  [G2] 2025/11/30      3:25.1  │
│          ジャパンカップ            │
│  ...                              │
└───────────────────────────────────┘
```

**結果リストアイテム（変更後のレイアウト）:**

```
┌──────────────────────────────────────┐
│ [着順] [grade] [日付       ] [タイム] │
│                [レース名    ]        │
└──────────────────────────────────────┘
```

| 要素 | 表示ルール |
|---|---|
| 着順 | 1着=金, 2着=銀, 3着=銅, 4着以下=グレー。幅28px固定 |
| gradeバッジ | ホーム画面と同じスタイル。gradeがない場合はスペースを詰める |
| 日付 | `YYYY/MM/DD` 形式。シアン色。fontSize 11 |
| レース名 | 白色、太字。省略なし（改行される） |
| タイム | 白70%透過。等幅数字 |

**最大高さ:** 画面高さの75%

### 6.6 馬名一覧ダイアログ (HorseNameListDialog)

**Widgetの種別:** ConsumerWidget

**起動条件:** 検索バーの一覧ボタン（📋）をタップ

**ダイアログ構成:**

```
┌─────────────────────────────┐
│ ┌─────────────────────────┐ │
│ │ 馬名一覧（あいうえお順） │ │  ← ヘッダー
│ └─────────────────────────┘ │
│                             │
│ ┌─ ア行 ─────────────────┐ │  ← グループヘッダー
│ │ アーモンドアイ     15走  │ │     (シアン色、半透明背景)
│ │ アカイイト          8走  │ │
│ │ アグネスタキオン     5走 │ │  ← 非アクティブ(グレー)
│ └─────────────────────────┘ │
│ ┌─ カ行 ─────────────────┐ │
│ │ ...                     │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

**グループ化の仕様:**

| グループ | 含まれる先頭文字 |
|---|---|
| ア行 | ア, イ, ウ, エ, オ |
| カ行 | カ, キ, ク, ケ, コ, ガ, ギ, グ, ゲ, ゴ |
| サ行 | サ, シ, ス, セ, ソ, ザ, ジ, ズ, ゼ, ゾ |
| タ行 | タ, チ, ツ, テ, ト, ダ, ヂ, ヅ, デ, ド |
| ナ行 | ナ, ニ, ヌ, ネ, ノ |
| ハ行 | ハ, ヒ, フ, ヘ, ホ, バ, ビ, ブ, ベ, ボ, パ, ピ, プ, ペ, ポ |
| マ行 | マ, ミ, ム, メ, モ |
| ヤ行 | ヤ, ユ, ヨ |
| ラ行 | ラ, リ, ル, レ, ロ |
| ワ行 | ワ, ヲ, ン |
| その他 | 上記に該当しない先頭文字 |

**アクティブ判定:** `最終出走年 >= 今年 - 1`

| 状態 | 馬名の色 | 出走数の色 |
|---|---|---|
| アクティブ | `Colors.white` | `Colors.white70` |
| 非アクティブ | `Colors.white38` | `Colors.white24` |

**タップ時:** HorseSearchResultDialog を表示（その馬の成績を検索）

**最大高さ:** 画面高さの85%

### 6.7 着順推移グラフ (ResultChartPainter)

**描画方式:** CustomPainter（Flutterのキャンバス描画API）

**表示条件:** 馬名検索結果が2件以上の場合のみ表示

**グラフ仕様:**

```
1  ──●──────────────────────  ← グリッド線 (白10%)
2  ──────●──────────────────
3  ──────────────────────●──
:        :              :
20 ──────────────────────────
   2025   2025   2026
   06-01  12-28  01-05
```

| 仕様 | 内容 |
|---|---|
| Y軸 | 1〜20（1着が上、20着が下）。1刻みのグリッド線 |
| X軸 | データポイントを均等配置 |
| 折れ線 | シアン色、太さ2.5px、角を丸く |
| ドット | 半径5px。1着=金, 2着=銀, 3着=銅, その他=シアン |
| ドット枠線 | 白15%透過、太さ1px |

**X軸ラベル（凡例）の仕様:**

| 仕様 | 内容 |
|---|---|
| 表示形式 | 2行表示: 1行目=年(YYYY)、2行目=月-日(MM-DD) |
| 表示間隔 | 10件以下: 全件表示。11件以上: 約8分の1間隔 |
| 例外 | 最初と最後のデータは必ず表示 |
| フォントサイズ | 9px、行間1.3倍 |
| 中央揃え | ドットの真下に中央揃え |

**コンテナ仕様:**

| 仕様 | 値 |
|---|---|
| 高さ | 340px |
| 背景色 | `#0F0F1A` |
| 内余白 | 上12px、左28px（Y軸ラベル分）、右12px、下40px（2行ラベル分） |
| 下マージン | 12px（リストとの間隔） |

---

## 7. API連携仕様

> **このセクションの目的：** サーバーとの通信の「契約」を定義する。フロントエンドとバックエンドの開発者が、このセクションを見ればお互いの期待するリクエスト/レスポンスを理解できる。

### 7.1 共通仕様

| 項目 | 値 |
|---|---|
| ベースURL | `http://49.212.175.205:8080` |
| 通信方式 | HTTP GET |
| データ形式 | JSON |
| 文字コード | UTF-8 |
| エラー時 | ステータスコード200以外で例外をスロー |

### 7.2 エンドポイント一覧

#### GET /getAllRace

```
目的:     全レース一覧の取得
パラメータ: なし
レスポンス: HourseRaceList の JSON配列
使用Provider: allRaceProvider
```

レスポンス例:
```json
[
  {
    "id": 1,
    "year": "2026",
    "month": "01",
    "day": "05",
    "grade": "g1",
    "race_name": "有馬記念",
    "place": "中山",
    "age_rate": "3歳以上",
    "course_kind": "芝",
    "course_length": "2500",
    "result_url": "https://...",
    "finish": 1
  }
]
```

#### GET /getAllHourseNames

```
目的:     全馬名リストの取得（名前のみ）
パラメータ: なし
レスポンス: String の JSON配列
使用Provider: allHourseNamesProvider
```

レスポンス例:
```json
["アーモンドアイ", "アカイイト", "イクイノックス"]
```

#### GET /getAllHourseNamesWithStats

```
目的:     全馬名リストの取得（統計情報付き）
パラメータ: なし
レスポンス: HourseNameStats の JSON配列
使用Provider: allHourseNamesWithStatsProvider
```

レスポンス例:
```json
[
  {
    "hourse_name": "アーモンドアイ",
    "race_count": 15,
    "last_year": "2025"
  }
]
```

#### GET /getSelectedResult

```
目的:     特定レースの結果取得
パラメータ:
  - year      (String) 開催年
  - month     (String) 開催月
  - day       (String) 開催日
  - race_name (String) レース名
レスポンス: HourseRaceResult の JSON配列
使用Provider: selectedResultProvider
注意: レスポンスが null の場合は空配列として扱う
```

レスポンス例:
```json
[
  {
    "id": 100,
    "year": "2026",
    "month": "01",
    "day": "05",
    "grade": "g1",
    "race_name": "有馬記念",
    "result": 1,
    "hourse_name": "ドウデュース",
    "age": "4",
    "jockey_name": "武豊",
    "race_time": "2:32.4"
  }
]
```

#### GET /getResultByHourseName

```
目的:     馬名による成績検索（LIKE部分一致）
パラメータ:
  - hourse_name (String) 馬名
レスポンス: HourseRaceResult の JSON配列
使用Provider: resultByHourseNameProvider
注意: サーバー側でLIKE検索（部分一致）を行う
     レスポンスが null の場合は空配列として扱う
```

---

## 8. 画面遷移図

> **このセクションの目的：** ユーザーがアプリ内でどういう経路をたどるかを図示する。「この画面からどこに行けるか」を一覧できる。

```
アプリ起動
    │
    ▼
┌──────────┐
│ホーム画面  │
│(HomeScreen)│
└──────────┘
    │
    ├── レースをタップ ──────────────────────┐
    │                                       ▼
    │                              ┌──────────────────┐
    │                              │ レース結果ダイアログ │
    │                              │ (RaceResultDialog) │
    │                              └──────────────────┘
    │
    ├── 検索実行（🔍 or Enter） ──────────────┐
    │                                         ▼
    │                              ┌────────────────────────┐
    │                              │ 馬名検索結果ダイアログ    │
    │                              │(HorseSearchResultDialog)│
    │                              │ ┌────────────────────┐  │
    │                              │ │着順推移グラフ        │  │
    │                              │ │(ResultChartPainter) │  │
    │                              │ └────────────────────┘  │
    │                              └────────────────────────┘
    │
    ├── サジェストをタップ ──────────────────── (同上)
    │
    └── 一覧ボタン（📋）───────────────────┐
                                           ▼
                                ┌────────────────────┐
                                │ 馬名一覧ダイアログ   │
                                │(HorseNameListDialog)│
                                └────────────────────┘
                                    │
                                    └── 馬名をタップ ──┐
                                                       ▼
                                           ┌────────────────────────┐
                                           │ 馬名検索結果ダイアログ    │
                                           │(HorseSearchResultDialog)│
                                           └────────────────────────┘
```

**遷移方式:** すべて `showDialog` によるモーダルダイアログ。
戻りはダイアログ外タップまたはシステムの戻るボタン。

---

## 9. ビルド手順

> **このセクションの目的：** 開発環境のセットアップ方法を残す。「動かない」を防ぐ。

### 9.1 コード生成

モデルやProviderを変更した場合は、以下のコマンドでコード生成が必要:

```bash
cd test_flutter_claude_code_test3
flutter pub run build_runner build --delete-conflicting-outputs
```

> **注意:** `dart run` ではなく `flutter pub run` を使うこと。
> asdf経由の `dart` コマンドはバージョンが古い (3.8.1) ため、
> Flutter同梱のDart (3.10.8) を使う必要がある。

### 9.2 生成されるファイル

| 元ファイル | 生成ファイル | 内容 |
|---|---|---|
| `hourse_race_list.dart` | `.freezed.dart`, `.g.dart` | freezed + JSON変換 |
| `hourse_race_result.dart` | `.freezed.dart`, `.g.dart` | freezed + JSON変換 |
| `hourse_name_stats.dart` | `.freezed.dart`, `.g.dart` | freezed + JSON変換 |
| `race_provider.dart` | `.g.dart` | Riverpod Provider |

### 9.3 実機ビルド

```bash
cd test_flutter_claude_code_test3
flutter run            # デバッグビルド＆実行
flutter build apk      # APK生成 (Android)
```

---

## 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.0 | 2026-02-08 | 初版作成 |
